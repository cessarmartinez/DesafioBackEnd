<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="/static/css/{{style}} " />
        <title>{{title}}</title>
        <!-- Link Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    </head>
    <body>
        <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-lav">
            <div class="container-fluid">
              <button
                class="navbar-toggler"
                type="button"
                data-bs-toggle="collapse"
                data-bs-target="#navbarNav"
                aria-controls="navbarNav"
                aria-expanded="false"
                aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
              </button>
              <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav">
                {{#unless loged}}
                  <li class="nav-item">
                    <a class="nav-link active text-light" aria-current="page" href="/login">Login</a>
                  </li>
                  <li class="nav-item">
                    <a class="nav-link active text-light" href="/register">Register</a> 
                  </li>
                {{/unless}}
                {{#if loged}}
                <a class="nav-link active text-light" aria-current="page" href="/">Inicio</a>
                <a class="nav-link active text-light" aria-current="page" href="/api/session/logout">LogOut</a>
                <a class="nav-link active text-light" aria-current="page" href="/profile">Perfil</a>
                <a class="nav-link active text-light" aria-current="page" href="/chat">Chat</a>
                {{/if}}
                {{#if role}}
                <a class="nav-link active text-light" aria-current="page" href="/realTimeProducts">Productos en tiempo real</a>
                <li><a class="nav-link active text-light" aria-current="page" href="/api/users">Usuarios</a></li>
                {{/if}}
                </ul>
              </div>
            </div>
          </nav>
    </header>
        {{{body}}}
    </body>
 <!-- Link Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</html>